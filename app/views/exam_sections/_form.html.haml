= simple_form_for(@exam_section) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :dificultyRating
    - if @mcqs.present?
      %strong MCQs:
      .inline-checkbox= f.collection_check_boxes :mcqs, @mcqs, :id, :to_html_safe
    - else
      %strong There are no free mcqs.

    %br

  .form-actions
    = f.button :submit , class: 'btn btn-default'
