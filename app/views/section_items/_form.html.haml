= simple_form_for([@parent_resource, @section, @section_item]) do |f|
  = f.error_notification
  .form-inputs
    = f.association :mcq
  .form-actions
    = f.button :submit , class: 'btn btn-default'
