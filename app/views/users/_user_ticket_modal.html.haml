%script{:src => "//ajax.aspnetcdn.com/ajax/jquery.validate/1.9/jquery.validate.min.js"}
#UserTicketModal.modal-input-edit.modal.latest_pop.fade{:role => "dialog"}
  .modal-dialog.modal-lg{:role => "document"}
    .modal-content
      = simple_form_for(Ticket.new, html: {class: 'ticket-form new_ticket_form'}) do |f|
        .modal-header
          %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
            %span{"aria-hidden" => "true"} ×
          %h4.modal-title New Ticket
        .modal-body
          = hidden_field_tag :is_admin_ticket, true
          = render 'tickets/form_inputs', f: f, locals: {asker_id: locals[:asker_id]}
          = hidden_field_tag "type", "follow_up_required"
          = f.hidden_field :asker_id, value: locals[:asker_id]
        .modal-footer
          = f.submit class: "btn btn-success"
