= simple_form_for([@exercise, @mcq_attempt]) do |f|
  = f.error_notification

  .form-inputs
    = f.association :mcq_stem
    = f.association :mcq
    = f.association :mcq_answer

  .form-actions
    = f.button :submit, class: 'btn btn-default'
