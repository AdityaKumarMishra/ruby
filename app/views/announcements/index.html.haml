%h1 Announcements

%h4 Filter Product Versions
.panel.panel-default
  .panel-body
    = form_for_filterrific @filterrific do |f|
      .form-group
        %label Type
        =f.select(:by_product_line, @filterrific.select_options[:by_product_line],{selected: 'All' }, class: 'form-control')
      = link_to 'Reset filters', reset_filterrific_url, class: 'btn btn-default'

= link_to 'Create Announcement',new_announcement_path, class: 'btn btn-primary u-white-link'
%br
%br
%br
=render(partial: 'announcements/table', locals: { all_announcements: @all_announcements, pending_announcements: @all_pending_announcements})

