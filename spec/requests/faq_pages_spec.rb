require 'rails_helper'

RSpec.describe "FaqPages", type: :request do
  describe "GET /faq_pages" do
    it "works! (now write some real specs)" do
      # get gamsat_admin_faq_pages_path, {type: 'gamsat'}
      # expect(response).to have_http_status(200)

      # Tests commented out because they are not valid.
      # Admin FAQ pages require authentication
      # TODO
    end
  end
end
