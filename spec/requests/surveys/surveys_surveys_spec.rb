require 'rails_helper'

RSpec.describe "Surveys::Surveys", type: :request do
  describe "GET /surveys_surveys" do
    pending "works! (now write some real specs)" do
      get surveys_surveys_path
      expect(response).to have_http_status(200)
    end
  end
end
