require 'rails_helper'

RSpec.describe "Surveys::SurveyAnswers", type: :request do
  describe "GET /surveys_survey_answers" do
    pending "works! (now write some real specs)" do
      get surveys_survey_answers_path
      expect(response).to have_http_status(200)
    end
  end
end
