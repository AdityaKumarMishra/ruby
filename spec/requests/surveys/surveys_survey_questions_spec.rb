require 'rails_helper'

RSpec.describe "Surveys::SurveyQuestions", type: :request do
  describe "GET /surveys_survey_questions" do
    pending "works! (now write some real specs)" do
      get surveys_survey_questions_path
      expect(response).to have_http_status(200)
    end
  end
end
